/**
 * Later slices (not implemented in this build):
 * Outlook mail, GitLab, and AI.
 */
export const deferredSliceNotes = {
  slice5: "Jira read-only assigned issues (done)",
  slice6: "Zoho Desk open tickets (done, read-only)",
  slice7: "GitHub issues, pull requests, and reviews (done, read-only)",
  slice8: "Outlook inbox triage, then AI classification",
} as const;
